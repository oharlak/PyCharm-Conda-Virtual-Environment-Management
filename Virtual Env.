#install a package
conda install a_package_name

#install a specified package
conda install a_package_name = version

#remove an installed package
conda remove a_package_name

#list all installed packages
conda list

